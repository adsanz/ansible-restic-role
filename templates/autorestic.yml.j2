# GENERATED BY ANSIBLE - RESTIC ROLE

locations:
{% for locs in restic_locations %}
    {{ locs | to_nice_yaml(indent=1) | indent(4) }}
{%- endfor %}

backends:
{% for backs in restic_backends %}
    {{ backs | to_nice_yaml(indent=1) | indent(4) }}
{%- endfor %}

