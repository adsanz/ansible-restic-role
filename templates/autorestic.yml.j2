# GENERATED BY ANSIBLE - RESTIC ROLE

locations:
    {{ restic_locations | to_nice_yaml(indent=2) | trim | indent(6) }}
backends:
    {{ restic_backends | to_nice_yaml(indent=2) | trim | indent(6) }}
